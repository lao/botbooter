# Local environment
.env
.env.*

# WhatsApp Web (whatsmeow) session store — holds credentials, never commit.
botbooter-whatsapp*.db*

todo.md


# Test & coverage artifacts
coverage.txt
*.out
*.test

# Build artifacts
/bin/
/dist/
# Compiled example binary (go build ./_examples/v1); anchored so it does not
# match the _examples/v1 source directory.
/v1

# Editor / OS noise
.DS_Store
.idea/
.vscode/
.claude/
.plans/
_docs/superpowers/
