# Local environment
.env
.env.*
todo.md
SPEC.md
*.pem

# WhatsApp Web (whatsmeow) session store — holds credentials, never commit.
botbooter-whatsapp*.db*

# Test & coverage artifacts
coverage.txt
*.out
*.test

# Build artifacts
/bin/
/dist/
# Compiled example binary (go build ./_examples/basic); anchored so it does not
# match the _examples/basic source directory.
/basic

# Editor / OS noise
.DS_Store
.idea/
.vscode/
.claude/
.plans/
_docs/superpowers/

# Working documents (plans/specs) — kept local, not part of the library
docs/superpowers/
